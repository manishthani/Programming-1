#include <iostream>
#include <vector>
using namespace std;

typedef vector<vector<int> > Matrix;

int main(){
	int n, m;
	cin >> n >> m;
	Matrix A (n, vector<int> (m));
	for(int i = 0; i < n; ++i){
		for(int j = 0; j < m; ++j){
			cin >> 
		}
	}
}